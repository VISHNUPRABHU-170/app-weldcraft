import { Routes } from '@angular/router';
import { HomeComponent } from './modules/home/components/view/view.component';

export const routes: Routes = [
  {path: 'home', component: HomeComponent}
];
